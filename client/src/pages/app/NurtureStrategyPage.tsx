import React from 'react';
import DocViewPage from './DocViewPage';

export default function NurtureStrategyPage() {
  return (
    <DocViewPage
      kind="nurture_strategy"
      title="Nurture strategy"
      subtitle="The journey every lead follows — from first touch to your call. Cold → Warming → Hot → Ready."
    />
  );
}
